input([(ngModel)]='search', type='text', placeholder='Search by User first name..')#searchInput
.table-responsive-md
  table.table
    thead.thead-dark
      tr
        th(scope='col') #
        th(scope='col') First Name
        th(scope='col') Last Name
        th(scope='col') Email
        th(scope='col') Options
    tbody
      tr(*ngFor='let user of users|tableSearch: search; let i = index')
        th(scope='row') {{i}}
        td {{user.first_name}}
        td {{user.last_name}}
        td {{user.email}}
        td
          a(href='javascript:void(0)')
            i.fa.fa-edit.text-warning
          a(href='javascript:void(0)', (click)='deleteUser(user._id, $event)')
            i.text-danger.fa.fa-trash
          a(href='')
            i.fa.fa-info.text-primary
